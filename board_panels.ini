	dialog = vssFilterSettings, "Spike Filter"
		field = "Enable VSS Spike Filter", devBit0
		field = "Max frequency jump ratio", scriptSetting8, {devBit0}

	dialog = vssFilterDialog, "Vehicle Speed Sensor", yAxis
		panel = speedSensorAnalog
		panel = vssFilterSettings
